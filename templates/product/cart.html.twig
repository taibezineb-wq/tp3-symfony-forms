<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Confirmation - EHEI TP3</title>
    
    <!-- Bootstrap 5.3 CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <style>
        body {
            background-color: #f0f8ff;
            padding-top: 20px;
        }
        .order-card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            overflow: hidden;
        }
        .order-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 20px;
        }
        .product-image-cart {
            width: 100%;
            height: 300px;
            object-fit: cover;
            border-radius: 10px;
        }
        .list-group-item {
            border: none;
            padding: 15px 0;
            border-bottom: 1px solid #eee;
        }
        .total-price {
            font-size: 1.5rem;
            color: #198754;
            font-weight: bold;
        }
        .back-btn {
            background-color: #6c757d;
            border-color: #6c757d;
        }
        .back-btn:hover {
            background-color: #5a6268;
            border-color: #545b62;
        }
    </style>
</head>
<body>
    <div class="container my-5">
        <div class="row justify-content-center">
            <div class="col-lg-10">
                <div class="order-card">
                    <!-- En-tête -->
                    <div class="order-header text-center">
                        <h1 class="mb-3">🎉 Order Confirmed!</h1>
                        <p class="lead mb-0">Thank you for your purchase!</p>
                    </div>
                    
                    <!-- Corps -->
                    <div class="card-body p-5">
                        <div class="row mb-5">
                            <div class="col-md-8">
                                <h2 class="card-title">{{ product.name }}</h2>
                                <p class="text-muted">Order #{{ random(1000, 9999) }} • {{ "now"|date("d/m/Y H:i") }}</p>
                            </div>
                            <div class="col-md-4 text-end">
                                <div class="total-price">
                                    Total: ${{ total|number_format(2, '.', ',') }}
                                </div>
                            </div>
                        </div>
                        
                        <div class="row">
                            <!-- Image produit -->
                            <div class="col-md-5">
                                <img src="{{ product.image }}" 
                                     class="product-image-cart" 
                                     alt="{{ product.name }}">
                            </div>
                            
                            <!-- Détails commande -->
                            <div class="col-md-7">
                                <h4 class="mb-4">Order Details</h4>
                                
                                <ul class="list-group list-group-flush">
                                    <li class="list-group-item">
                                        <div class="row">
                                            <div class="col-6"><strong>Quantity:</strong></div>
                                            <div class="col-6 text-end">{{ quantity }}</div>
                                        </div>
                                    </li>
                                    <li class="list-group-item">
                                        <div class="row">
                                            <div class="col-6"><strong>Color Selected:</strong></div>
                                            <div class="col-6 text-end">{{ color }}</div>
                                        </div>
                                    </li>
                                    <li class="list-group-item">
                                        <div class="row">
                                            <div class="col-6"><strong>Unit Price:</strong></div>
                                            <div class="col-6 text-end">${{ product.price|number_format(2, '.', ',') }}</div>
                                        </div>
                                    </li>
                                    <li class="list-group-item">
                                        <div class="row">
                                            <div class="col-6"><strong>Subtotal:</strong></div>
                                            <div class="col-6 text-end">${{ (product.price * quantity)|number_format(2, '.', ',') }}</div>
                                        </div>
                                    </li>
                                    <li class="list-group-item">
                                        <div class="row">
                                            <div class="col-6"><strong>Shipping:</strong></div>
                                            <div class="col-6 text-end text-success">FREE</div>
                                        </div>
                                    </li>
                                </ul>
                                
                                <div class="mt-4">
                                    <h5>Product Specifications:</h5>
                                    <ul class="list-unstyled">
                                        <li><strong>Brand:</strong> {{ product.brand }}</li>
                                        <li><strong>Connectivity:</strong> {{ product.connectivity }}</li>
                                        <li><strong>Battery Life:</strong> {{ product.batteryLife }}</li>
                                        <li><strong>Warranty:</strong> 2 Years</li>
                                    </ul>
                                </div>
                                
                                <div class="alert alert-info mt-4">
                                    <h6>Shipping Information</h6>
                                    <p class="mb-0">Your order will be processed within 24 hours and shipped within 2-3 business days. 
                                    You will receive a tracking number via email once your order ships.</p>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Boutons -->
                        <div class="row mt-5">
                            <div class="col-md-6">
                                <a href="{{ path('app_product') }}" class="btn btn-secondary btn-lg back-btn">
                                    ← Back to Product
                                </a>
                            </div>
                            <div class="col-md-6 text-end">
                                <button class="btn btn-success btn-lg">
                                    Print Receipt
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>